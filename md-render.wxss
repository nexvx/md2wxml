/**
 * 美化版 Markdown 渲染组件样式 - 深色主题
 * 丰富多彩的深色主题，适配 rgb(37, 37, 38) 背景
 */

.md-render {
  font-size: 31rpx;
  line-height: 2;
  color: rgba(255, 255, 255, 0.9);
  word-wrap: break-word;
  word-break: break-all;
  padding: 10rpx;
  border-radius: 12rpx;
}

/* 标题样式 - 渐变色彩 */
.md-heading {
  font-weight: 700;
  margin: 20rpx 0 15rpx 0;
  line-height: 1.4;
  position: relative;
}

.md-h1 {
  font-size: 51rpx;
  background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  border-bottom: 4rpx solid;
  border-image: linear-gradient(90deg, #4facfe, #00f2fe, transparent) 1;
  padding-bottom: 20rpx;
  margin-top: 48rpx;
  text-shadow: 0 0 20rpx rgba(79, 172, 254, 0.3);
}

.md-h2 {
  font-size: 43rpx;
  background: linear-gradient(135deg, #ff6b6b 0%, #feca57 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  border-bottom: 3rpx solid;
  border-image: linear-gradient(90deg, #ff6b6b, #feca57, transparent) 1;
  padding-bottom: 16rpx;
}

.md-h3 {
  font-size: 37rpx;
  background: linear-gradient(135deg, #a8e6cf 0%, #88d8c0 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.md-h4 {
  font-size: 33rpx;
  background: linear-gradient(135deg, #c084fc 0%, #a855f7 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.md-h5 {
  font-size: 29rpx;
  background: linear-gradient(135deg, #f472b6 0%, #ec4899 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.md-h6 {
  font-size: 27rpx;
  background: linear-gradient(135deg, #94a3b8 0%, #64748b 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* 段落样式 - 添加颜色变化 */
.md-paragraph {
  margin: 20rpx 0rpx 15rpx 0rpx;
  text-align: justify;
  color: rgba(255, 255, 255, 0.9);
}

/* 行内元素容器 */
.md-inline-wrapper {
  display: inline;
}

/* 文本样式 */
.md-text {
  display: inline;
}

/* 粗体 - 黄色强调 */
.md-bold {
  font-weight: bold;
  display: inline;
  color: #fbbf24;
  text-shadow: 0 0 10rpx rgba(251, 191, 36, 0.3);
}

/* 斜体 - 紫色优雅 */
.md-italic {
  font-style: italic;
  display: inline;
  color: #c084fc;
  text-shadow: 0 0 8rpx rgba(192, 132, 252, 0.2);
}

/* 删除线 - 红色 */
.md-strike {
  text-decoration: line-through;
  color: rgba(239, 68, 68, 0.7);
  display: inline;
}

/* 行内代码 - 青色背景 */
.md-inline-code {
  background: linear-gradient(135deg, rgba(6, 182, 212, 0.15) 0%, rgba(14, 165, 233, 0.15) 100%);
  color: #06b6d4;
  padding: 4rpx 16rpx;
  border-radius: 8rpx;
  font-family: 'SF Mono', Consolas, Monaco, 'Courier New', monospace;
  font-size: 27rpx;
  display: inline;
  border: 1rpx solid rgba(6, 182, 212, 0.3);
  box-shadow: 0 0 10rpx rgba(6, 182, 212, 0.2);
}

/* 链接 - 蓝色霓虹 */
.md-link {
  color: #4facfe;
  text-decoration: none;
  display: inline;
  transition: all 0.3s ease;
  text-shadow: 0 0 10rpx rgba(79, 172, 254, 0.5);
}

.md-link:hover {
  color: #00f2fe;
  text-decoration: underline;
  text-shadow: 0 0 15rpx rgba(0, 242, 254, 0.8);
}

/* 图片 */
.md-image {
  max-width: 100%;
  height: auto;
  margin: 32rpx 0;
  border-radius: 12rpx;
  box-shadow: 0 4rpx 12rpx rgba(0, 0, 0, 0.3);
  display: block;
}

/* 代码块 */
.md-codeblock {
  background-color: #1e1e1e;
  border-radius: 16rpx;
  margin: 32rpx 0;
  overflow: hidden;
  box-shadow: 0 8rpx 24rpx rgba(0, 0, 0, 0.15);
}

.md-codeblock-lang {
  background-color: #2d2d2d;
  color: #9ca3af;
  font-size: 25rpx;
  padding: 12rpx 24rpx;
  border-bottom: 1rpx solid #3e3e3e;
}

.md-codeblock-scroll {
  padding: 24rpx;
  overflow-x: auto;
  white-space: pre;
}

.md-codeblock-text {
  color: #d4d4d4;
  font-family: 'SF Mono', Consolas, Monaco, 'Courier New', monospace;
  font-size: 27rpx;
  line-height: 1.7;
}

/* 引用块 - 多彩边框 */
.md-blockquote {
  border-left: 8rpx solid;
  border-image: linear-gradient(180deg, #10b981, #059669, #047857) 1;
  padding: 24rpx 32rpx;
  margin: 32rpx 0;
  background: linear-gradient(135deg, rgba(16, 185, 129, 0.1) 0%, rgba(5, 150, 105, 0.05) 100%);
  border-radius: 0 12rpx 12rpx 0;
  color: rgba(255, 255, 255, 0.9);
  font-style: italic;
  box-shadow: 0 4rpx 20rpx rgba(16, 185, 129, 0.1);
  position: relative;
}

.md-blockquote::before {
  content: '💡';
  position: absolute;
  top: 20rpx;
  right: 20rpx;
  font-size: 31rpx;
  opacity: 0.6;
}

/* 分隔线 - 彩虹渐变（去除黄色） */
.md-hr {
  border: none;
  height: 4rpx;
  background: linear-gradient(90deg, transparent, #4facfe, #00f2fe, #c084fc, #10b981, transparent);
  box-shadow: 0 0 10rpx rgba(79, 172, 254, 0.3);
  margin: 48rpx 0;
}

/* 列表样式 */
.md-list {
  margin: 24rpx 0;
}

.md-list-item {
  display: flex;
  align-items: flex-start;
  margin: 12rpx 0;
  line-height: 1.8;
}

.md-list-marker {
  flex-shrink: 0;
  width: 30rpx;
  text-align: center;
  font-weight: bold;
}

/* 无序列表 - 绿色系 */
.md-list-unordered .md-list-marker {
  font-size: 31rpx;
  line-height: 1.6;
  background: linear-gradient(135deg, #10b981, #059669);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* 有序列表 - 橙色系 */
.md-list-ordered .md-list-marker {
  font-size: 29rpx;
  background: linear-gradient(135deg, #fb923c, #f97316);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.md-list-content {
  flex: 1;
  min-width: 0;
}

/* 嵌套列表 */
.md-nested-list {
  margin-top: 12rpx;
}

.md-nested-list .md-list {
  margin: 0;
}

.md-nested-list .md-list-item {
  margin: 8rpx 0;
}

.md-nested-list .md-list-marker {
  width: 30rpx;
  font-size: 27rpx;
}

/* 嵌套无序列表使用不同的标记符号 */
.md-nested-list .md-list-unordered .md-list-marker {
  background: linear-gradient(135deg, #a855f7, #7c3aed);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* 动画效果 */
@keyframes glow {
  0%, 100% { text-shadow: 0 0 5rpx rgba(79, 172, 254, 0.5); }
  50% { text-shadow: 0 0 20rpx rgba(79, 172, 254, 0.8); }
}

.md-heading:hover {
  animation: glow 2s ease-in-out infinite;
}